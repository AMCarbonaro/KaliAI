name: recon_only
description: Passive reconnaissance only - no active probes or exploits
tool_whitelist:
  - nmap
  - passive_scanners
safety:
  require_confirmation: true
  allow_exploits: false
  allow_active_probes: false
  max_scan_intensity: low
reporting:
  style: detailed
  include_recommendations: true
focus:
  - port_scanning
  - service_detection
  - banner_grabbing
  - dns_enumeration

